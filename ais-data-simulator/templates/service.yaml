apiVersion: v1
kind: Service
metadata:
  name: {{ include "ais-data-simulator.fullname" . }}
  labels:
    {{- include "ais-data-simulator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ais-data-simulator.selectorLabels" . | nindent 4 }}
